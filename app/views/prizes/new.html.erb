<div class="create">
  <div class="container">
   	<%= form_for(@prize) do |f| %>
  		<div class="field">
        <%= f.label :Premio %><br>
    		<%= f.text_area :name %><br>
  		</div>
  		<div class="actions">
    		<%= f.submit "Create" %>
  		</div>
		<% end %>
  </div>
</div>
