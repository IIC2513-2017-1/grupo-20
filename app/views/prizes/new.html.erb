<div class="create">
  <div class="container">
   	<%= form_for(new_rifa_prize_path) do |f| %>
  		<div class="field">
        <%= f.label :Premio %><br>
    		<%= f.text_area :name %><br>
  		</div>
  		<div class="actions">
    		<%= f.submit "Create" %>
  		</div>
		<% end %>
  </div>
</div>
