<div class="create">
  <div class="container">
    <%= form_for(@session) do |f| %>
        <div class="field">
          <%= f.label :name %><br>
          <%= f.text_area :name %><br>
          <%= f.label :password %><br>
          <%= f.text_area :password%><br>
        </div>
        <div class="actions">
          <%= f.submit "Create" %>
        </div>
    <% end %>
  </div>
</div>