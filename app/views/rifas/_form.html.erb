<%= form_for(rifa, tiene_premio) do |f| %>
  <% if rifa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rifa.errors.count, "error") %> prohibited this rifa from being saved:</h2>

      <ul>
      <% rifa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha_inicio %>
    <%= f.datetime_select :fecha_inicio %>
  </div>

  <div class="field">
    <%= f.label :fecha_termino %>
    <%= f.datetime_select :fecha_termino %>
  </div>

  <div class="field">
    <%= f.label :titulo %>
    <%= f.text_field :titulo %>
  </div>

    <div class="field">
      <%= f.label 'Premio 1' %>
      <%= f.text_field :p %>
    </div>

    <div class="field">
      <%= f.label 'Premio 2' %>
      <%= f.text_field :p2 %>
    </div>

    <div class="field">
      <%= f.label 'Premio 3' %>
      <%= f.text_field :p3 %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
